<?xml version="1.0" encoding="utf-8"?>
<!--
	Preference layout for FontChooserPreference.
	Mirrors the structure of preference_theme.xml so it sits consistently in the settings list.
-->
<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:orientation="vertical"
	android:paddingVertical="8dp">

	<TextView
		android:id="@android:id/title"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:ellipsize="end"
		android:paddingStart="?android:attr/listPreferredItemPaddingStart"
		android:paddingEnd="?android:attr/listPreferredItemPaddingEnd"
		android:singleLine="true"
		android:textAppearance="?android:attr/textAppearanceListItem" />

	<androidx.recyclerview.widget.RecyclerView
		android:id="@+id/recycler_fonts"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:clipToPadding="false"
		android:orientation="horizontal"
		android:paddingHorizontal="?android:attr/listPreferredItemPaddingStart"
		android:paddingVertical="8dp"
		android:scrollbars="none" />

</LinearLayout>
